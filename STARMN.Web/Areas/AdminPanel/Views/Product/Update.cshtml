@{
    ViewData["Title"] = "Dashboard v3";
    Layout = "~/Areas/AdminPanel/Views/Shared/_Layout.cshtml";
}

<main class="app-main">
    <div class="app-content">
        <div class="container-fluid">

            <div class="col-lg-12">
                <div class="card card-primary card-outline mb-4">
                    <div class="card-header bg-primary">
                        <div class="card-title ">Ürün Güncelle</div>
                    </div>
                    <form action="Update" method="post" class="needs-validation" novalidate>
                        <div class="card-body">
                            <div class="row g-3">
                                <div class="col-md-12">
                                    <label for="validationCustom01" class="form-label">Ürün Id</label>
                                    <input name="Id" type="text" class="form-control" id="validationCustom01" value="@Model.Id" required />
                                </div>
                                <div class="col-md-12">
                                    <label for="validationCustom01" class="form-label">Ürün Adı</label>
                                    <input name="Adi" type="text" class="form-control" id="validationCustom01" value="@Model.Adi" required />
                                </div>
                                <div class="col-md-12">
                                    <label for="validationCustom01" class="form-label">Fiyat </label>
                                    <input name="Fiyat" type="text" class="form-control" id="validationCustom01" value="@Model.Fiyat" required />
                                </div>
                                <div class="col-md-12">
                                    <label for="validationCustom01" class="form-label">Stok </label>
                                    <input name="Stok" type="text" class="form-control" id="validationCustom01" value="@Model.Stok" required />
                                </div>
                                <div class="col-md-12">

                                    <select name="categoryId" class="form-control">
                                        <option value="0">Kategori Seçin</option>
                                        @foreach (var item in ViewBag.Kategoriler)
                                        {
                                            <option value="@item.Id">@item.Adi</option>
                                        }
                                    </select>
                                </div>

                                
                                <div class="col-md-12">
                                    <label for="validationCustom02" class="form-label">Ürün Açıklaması</label>
                                    <textarea name="Aciklama" class="form-control" id="validationCustom02" placeholder="Açıklama" value="@Model.Aciklama" style="height: 6rem" required></textarea>
                                </div>
                            </div>
                        </div>
                        <div class="card-footer">
                            <button class="btn btn-info" type="submit">Ürün Güncelle</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</main>

@section Scripts {
    <script src="~/adminlte/dist/js/validationtemplate.js"></script>
}